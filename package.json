{
  "name": "edtechproject",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
     "build": "cd edtechclient && npm install && npm run build",
    "lint": "eslint .",
    "preview": "vite preview",
    "start": "npx concurrently \"npm run server\" \"npm run client\"",
    "server": "cd edtechserver && npm start",
    "client": "cd edtechclient && npm run dev"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
